import {
  Header,
  CategoryStrip,
  Hero,
  Marquee,
  ProductGrid,
  Banner,
  Stats,
  Newsletter,
  Footer,
} from '../ui';
import { SeriesGrid } from './SeriesGrid';
import { Consultation } from './Consultation';

export default ({ storeName, tagline, bannerText, products, categories }) => {
  const series = (id) =>
    products.filter((p) =>
      (p.category && p.category.toLowerCase() === id.toLowerCase()) ||
      (p.name || '').toLowerCase().includes(id.toLowerCase())
    );
  const play = series('play');
  const lumen = series('lumen');
  const rest = products.filter((p) => !play.includes(p) && !lumen.includes(p));

  return (
    <>
      <Header
        storeName={storeName}
        links={[
          { label: 'Séries PLAY', to: '#play' },
          { label: 'Séries LUMEN', to: '#lumen' },
          { label: 'Promos', to: '#promo' },
          { label: 'Suivi de commande', to: '/track' },
        ]}
      />

      <Hero
        eyebrow="Assemblé & testé — en stock"
        title={bannerText}
        lede={tagline}
        primary="Configurer mon PC"
        primaryTo="#play"
        secondary="Découvrir LUMEN"
        secondaryTo="#lumen"
      />

      <Marquee items={[
        'Paiement à la livraison',
        'Livraison 24/48h',
        'Support 7/7',
        'Garantie 12 mois',
        'Retour sous 14 jours',
      ]} />

      <CategoryStrip categories={categories} />

      <SeriesGrid
        id="play"
        title="Série PLAY"
        subtitle="Puissance brute pour le FPS et l'esport — configs optimisées, refroidissement soigné."
        products={play}
      />
      <SeriesGrid
        id="lumen"
        title="Série LUMEN"
        subtitle="Showpieces RGB & moniteurs — l'expérience visuelle au maximum."
        products={lumen}
      />

      {rest.length > 0 && <ProductGrid title="Tout voir" products={rest} columns={4} />}

      <Banner
        id="promo"
        eyebrow="Promos du moment"
        title="Jusqu'à -20% sur les configs PLAY"
        text="Stock limité — profitez-en avant la fin."
        to="#play"
        cta="En profiter"
      />

      <Consultation />

      <Stats items={[
        { value: '500+', label: 'Configs livrées' },
        { value: '4.9/5', label: 'Note clients' },
        { value: '24/48h', label: 'Livraison' },
        { value: '12 mois', label: 'Garantie' },
      ]} />

      <Newsletter
        title="Ne ratez aucun drop"
        text="Restocks, promos et nouveautés — un email par semaine, jamais plus."
        button="Rejoindre"
      />

      <Footer
        storeName={storeName}
        columns={[
          {
            title: 'Boutique',
            links: [
              { label: 'Série PLAY', to: '#play' },
              { label: 'Série LUMEN', to: '#lumen' },
              { label: 'Promos', to: '#promo' },
              { label: 'Panier', to: 'cart' },
            ],
          },
          {
            title: 'Compte',
            links: [
              { label: 'Se connecter', to: '/auth/login' },
              { label: 'Créer un compte', to: '/auth/register' },
              { label: 'Mes commandes', to: '/track' },
            ],
          },
          {
            title: 'Aide',
            links: [
              { label: 'Suivi de commande', to: '/track' },
              { label: 'Paiement sécurisé', to: '/checkout' },
              { label: 'Retour à la boutique', to: '/' },
            ],
          },
        ]}
      />
    </>
  );
};
